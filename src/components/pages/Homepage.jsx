const Homepage = () => {
  return (
    <div className="homepage-container">
      <h1>React Widgets!</h1>

      <div>Select a link to use a widget!</div>
    </div>
  );
};

export default Homepage;
